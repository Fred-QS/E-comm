{% extends 'base.html.twig' %}

{% block title %}Inscription{% endblock %}

{% block content %}
    <h1>Inscription</h1>

    {% if notification %}
        <div class="alert alert-{{ notify_state }}">
            {{ notification }}
        </div>
        <hr/>
    {% endif %}

    {{ form(form) }}
{% endblock %}
