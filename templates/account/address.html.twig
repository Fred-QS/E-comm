{% extends 'base.html.twig' %}

{% block title %}Mes adresses{% endblock %}

{% block content %}
    <a href="{{ path('app_account_add_address') }}" class="btn btn-info btn-sm float-right">Ajouter une adresse</a>
    <h1>Mes adresses</h1>
    <p>C'est dans cet espace que vous allez pouvoir gérer vos adresses.</p>
    <a href="{{ path('app_account') }}">Retourner à mon compte</a>
    <hr/>
    {% if app.user.addresses|length == 0 %}
        <p class="text-center">
            Vous n'avez pas encore ajouter d'adresse sur votre compte client. Pour en ajouter une, <a href="{{ path('app_account_add_address') }}">cliquez ici</a>
        </p>
    {% else %}
        <div class="row">
        {% for address in app.user.addresses %}
            <div class="col-md-4">
                <div class="card" style="width: 18rem;">
                    <div class="card-body">
                        <h5 class="card-title">{{ address.name }}</h5>
                        <p class="card-text">
                            {{ address.address }},<br/>
                            {{ address.postal }} {{ address.city }} - {{ address.country }}
                        </p>
                        <a href="{{ path('app_account_edit_address', {'id': address.id}) }}">Modifier</a> | <a href="{{ path('app_account_delete_address', {'id': address.id}) }}">Supprimer</a>
                    </div>
                </div>
            </div>
        {% endfor %}
        </div>
    {% endif %}
{% endblock %}
